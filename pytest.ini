[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = tests
markers =
    unit: fast mocked tests (default)
    integration: requires real Gemini + Supabase credentials (run with -m integration)
addopts = -m "not integration"
